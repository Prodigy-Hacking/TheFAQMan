#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../console-remote-client/bin/console.re" "$@"
  ret=$?
else 
  node  "$basedir/../console-remote-client/bin/console.re" "$@"
  ret=$?
fi
exit $ret
